
add model name in run.py
------------
"""*****Modified your models.py
Find this:
Pythonself.model = AutoModelForCausalLM.from_pretrained(    model_name,    torch_dtype=(torch.bfloat16 if torch.cuda.is_available() else torch.float32),)Show more lines

✅ Replace with THIS:
Pythonself.model = AutoModelForCausalLM.from_pretrained(    model_name,    torch_dtype=torch.bfloat16,    device_map="auto")Show more lines

????replaced in model.py for dynamic error
_past_length
Current error is NOT from WaW_aWa​
Your error is:
Plain TextTypeError: 'DynamicCache' object is not subscriptableShow more lines
It happens here:
Pythonk = past_key_values[0][0]Show more lines
inside:
Pythondef _past_length(past_key_values):Show more lines

Why this happened
For Qwen, HuggingFace may return old-style cache:
Pythonpast_key_values[0][0]Show more lines
But Gemma returns new-style HuggingFace cache:
PythonDynamicCacheShow more lines
So this old code breaks:
Pythonpast_key_values[0][0]   # ❌ fails for DynamicCacheShow more lines

✅ Fix: update _past_length() in models.py
Replace your current function:
Pythondef _past_length(past_key_values: Optional[Tuple]) -> int:    if not past_key_values:        return 0    k = past_key_values[0][0]    return k.shape[-2]






--------------------
"""***** in prompt.py
Find this line (around the error line 699):
Pythonassert "qwen" in args.model_name.lower(), "this prompt only for qwen models"Show more lines

🔧 Replace it with:
Pythonassert any(x in args.model_name.lower() for x in ["qwen", "gemma"]), \    "this prompt only supports qwen or gemma models"Show more lines

the above line is added i multiple places


